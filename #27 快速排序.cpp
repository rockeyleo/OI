#include <iostream>
using namespace std;

void sort(int a[],int left,int right){
    if(left>right){
        return;
    }
    int i = left;
    int j = right;
    key = a[0]
    while(i<j){
        while(i<j and key>=a[i])
        swap(a,i,j);
        while(i<j and key>=a[i])
        swap(a,i,j)
    }
    qsort(a,left,i-1);
    qsort(a,i+1,right);
}

int main(){
    int a[10];
    for(int i=0;i<=9;i++){
        cin>>a[i];
    }
}