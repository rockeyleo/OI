#include <iostream>
using namespace std;

void msort(int left,int right){
    int mid,k,i,j;
    if(left == right){
        return;
    }
    mid(left+right)/2
    msort(left,mid);
    msort(mid+1,right);
    i = left;
    j = mid+1;
    k = left;
    while(i<mid && j<=right){
        if(a[i]<=a[j]){
            r[k] = a[i];
            i++;k++;
        }else{
            
        }
    }
}

int main(){
    int a[100] r[100];
}